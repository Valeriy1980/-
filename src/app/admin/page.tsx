import { PageHeader } from "@/components/PageHeader";

export const metadata = { title: "Адмін-панель" };

export default function AdminPage() {
  return <PageHeader title="Адмін-панель" description="Загальний огляд." />;
}
